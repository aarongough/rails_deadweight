require File.dirname(__FILE__) + './spec_helper'

describe RubyDeadweight::ProjectGlobber do
  describe ".get_globbed_project" do
    before :each do
      
    end
    
    it "should return all the ruby files in a project's app dir globbed into a single string"
  end
end